package com.uren.catchu.SharePackage.Utils;

import android.content.Context;
import android.support.v4.content.ContextCompat;

public class ShareImgViewController {

    Context context;

    public ShareImgViewController(ContextCompat contextCompat, Context context){
        this.context = context;
    }

    public void xx(){
        /*publicShareImgv.setColorFilter(ContextCompat.getColor(context, R.color.background), android.graphics.PorterDuff.Mode.SRC_IN);
        publicShareTv.setTextColor(context.getResources().getColor(R.color.background, null));*/
    }
}
